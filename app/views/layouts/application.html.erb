<!DOCTYPE html>
<html>
<head>
	<title>Arquiteto</title>
	<%= stylesheet_link_tag    "application" %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
</head>
<body>
	<div id="body">
		<div id="menu"><%= render('layouts/menu') if user_signed_in? %></div>
		<div id="conteudo">
			<p class="notice"><%= notice %></p>
			<p class="alert"><%= alert %></p>

			<%= yield %>
		</div>		
	</div>
</body>
</html>
